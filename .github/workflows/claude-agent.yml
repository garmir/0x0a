name: Claude Agent Delegation - Performance_Optimization
on: [push, workflow_dispatch]

jobs:
  claude-agent:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: DeterminateSystems/nix-installer-action@main
        continue-on-error: true

      - name: Claude Agent - Performance_Optimization
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        continue-on-error: true
        run: |
          cd ~ && nix-shell -p nodejs expect --run 'expect -c "
            set timeout 900
            spawn npx @anthropic-ai/claude-code --dangerously-skip-permissions \"CLAUDE_Performance_Optimization\"
            expect { -re {.*bypass permissions.*} { send \"2\r\" } }
            expect { -re {.*analysis.*|.*complete.*|.*Write.*} { exit 0 } timeout { exit 1 } }
          "'

      - name: Ubuntu Fallback Agent
        if: failure()
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y nodejs npm curl
          echo "Ubuntu fallback: Performance_Optimization failed, documenting for retry" > fallback-0x0a.txt
          echo "Repository: ${{ github.repository }}" >> fallback-0x0a.txt
          echo "Task: CLAUDE_Performance_Optimization" >> fallback-0x0a.txt
          echo "Status: Needs retry with enhanced configuration" >> fallback-0x0a.txt
          curl -s ifconfig.me >> fallback-0x0a.txt

      - name: Upload Agent Results
        uses: actions/upload-artifact@v4
        with:
          name: claude-agent-Performance_Optimization-0x0a
          path: "*.txt"
          retention-days: 7